<nav class="fixed bottom-0 left-0 right-0 bg-white border-t-2 border-slate-800 shadow-[0_-4px_10px_rgba(0,0,0,0.05)] select-none z-50">
  <div class="max-w-md mx-auto flex justify-around items-center h-20">

    <% is_home = current_page?(owned_monsters_path) || current_page?(root_path) %>
    <%= link_to owned_monsters_path, class: "flex flex-col items-center justify-center w-full h-full gap-1 #{is_home ? 'text-fuchsia-500' : 'text-slate-600 hover:text-slate-800'}" do %>
      <span class="text-2xl <%= 'scale-110' if is_home %>">🏠</span>
      <span class="text-xs font-black tracking-wider">ホーム</span>
    <% end %>

    <div class="w-px h-8 bg-slate-200"></div>

    <% is_adventure = current_page?(new_adventure_path) %>
    
    <%= link_to adventure_navigation_path(current_user), class: "flex flex-col items-center justify-center w-full h-full gap-1 #{is_adventure ? 'text-fuchsia-500' : 'text-slate-600 hover:text-slate-800'}" do %>
      <span class="text-2xl">🗡️</span>
      <span class="text-xs font-black tracking-wider">冒険</span>
    <% end %>

  </div>
</nav>
